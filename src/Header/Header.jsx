import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => {
    return (
        <nav>
            {/* <Link className='mr-20' to='/'>Home</Link>
            <Link className='mr-20' to='/login'>Login</Link>
            <Link className='mr-20' to='/register'>Register</Link> */}
        </nav>
    );
};

export default Header;